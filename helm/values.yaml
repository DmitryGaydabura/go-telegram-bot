image:
  registry: "ghcr.io"
  repository: "dmitrygaydabura/go-telegram-bot"
  tag: "v1.0.0-36c7d2b-linux-amd64"
  os: "linux"
  arch: "amd64"
  pullPolicy: Always

service:
  type: ClusterIP
  port: 8080

ingress:
  enabled: false

resources: {}
